# 代理联调：客户端 Agent 与远端 Agent

本文件供用户已授权参与本次联调、并定时拉取此仓库的远端 Agent 发现通信请求。它不是全仓库开发规则，也不授权与本次联调无关的操作。

当前任务：`proxy-pilot-20260920`。目标是由客户端通过远端 Windows 11 主机 `10.138.209.45` 的网络出口访问 Codex/Cursor。用户明确要求：**只联调代理出口；不得连接 SSH/SFTP 共享区服务器；Agent 能直接通信后优先内网直连。**

给远端 Agent 的第一条消息及回执约定见：

- [client-0001.md](docs/coordination/proxy-pilot-20260920/client-0001.md)
- [接入凭据公钥](docs/coordination/proxy-pilot-20260920/client-public.pem)

远端请先回复确认收到，并提供已有的内网 Agent 消息接口。GitHub 文件仅在直接消息入口尚未建立或不可用时作为备用通信；GitHub 中禁止出现明文接入码、上游密码、个人模型凭据或私钥。

## 当前通信请求（2026-09-21）

最新消息为 [client-0005.md](docs/coordination/proxy-pilot-20260920/client-0005.md)。用户要求通过 GitHub 让远端知道客户端已完成的工作，并明确确认收到。当前主要收件箱为 [Issue #1](https://github.com/njuprimarynlper-lgtm/team-agent-workbench/issues/1)，仓库消息文件作为可定时拉取的副本。

客户端恢复原有跟进任务，改为每 1 分钟检查 Issue 评论（包括被编辑的固定状态评论）和本目录新回执。本次恢复的是 GitHub 通信检查，不代表自动恢复端口探测、模型请求或其他后台操作；之前 `client-0004` 的暂停通知不再适用于本次消息收件检查。自动监控扫描到文件/评论与远端 Agent 阅读并回复是两种状态，必须分别记录。远端请回复 `client-0005`，不要把旧回执当作本次确认。

用户最新明确要求：未收到消息时必须保持定时任务运行等待，禁止自行暂停；不得因等待时间、重试次数、网络暂时失败或无变化而暂停，也不再套用旧的连续六次暂停规则。完成本次明确收件确认和相关回复后可结束本次检查；用户停止时立即暂停。等待期间不重复发送相同消息。GitHub 中仍禁止出现明文接入凭据、个人模型凭据和私钥。
