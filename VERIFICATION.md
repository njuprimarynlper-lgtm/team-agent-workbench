# 2026-09-24 网络测试与成员转发权限分离

- 管理端三个网络测试不再受产品开关和出口启停限制，使用已保存的上游设置测试固定服务地址，不调用本机 CLI。成员请求仍按已保存的产品白名单拦截；测试不修改访问权限、不重启正在使用的出口、不计入成员隧道流量。
- 没有运行出口时，使用不开放监听端口的临时代理进程完成测试，成功或失败后释放；已有出口复用原进程。保留原有启动失败信息，测试成功不把出口标记成运行中。测试目标由 IPC 严格校验。
- 成员权限移到可见位置；独立测试区展示已保存的权限、各服务测试结果和未应用修改提示，明确无需在管理端安装或启动 CLI。保存后清除旧结果，包括仅修改代理密码的情况。
- 18 项相关后台用例通过，覆盖进程生命周期、关闭出口和产品禁用时的测试、成员权限仍拒绝、HTTP 403 与连接释放、生产后台进程 IPC、静态页面、监控与不可变资源构建。TypeScript 和隔离构建通过。桌面冒烟脚本已更新但未运行；未启动真实 Electron 窗口、访问外部模型服务、重启现有管理端或修改本机出口配置。

# 2026-09-24 会话跟随账号与旧窗口数据迁移

- 修复同一成员换窗口登录后看不到会话：用户端按服务器地址、端口、账号和指纹选取固定账号目录。同账号窗口共享一个 Workbench、写队列、CLI 运行实例和出口客户端；引用计数确保关闭一个窗口不会关闭另一个窗口的会话。窗口布局仍各自保存。
- 首次使用账号目录时，从旧根目录和所有数字窗口目录按记录归属迁移会话、整理、个人成果、输入和传输；恢复旧账号的待同步别名，复制关联附件、事件及该账号的 Codex 原生历史。原目录保持不变，先写完整临时目录再发布；迁移标记避免重新导入已删除记录。项目代码目录保持原路径，旧目录不应直接删除。
- 统一迁入记录的本地连接编号；验证成功后接管已验证的 SSH 连接，已有在线账号保留其运行任务。密码错误保持原账号，窗口关闭后迟到登录不能创建不可见账号，旧账号请求的迟到返回被拒绝。Codex 首次迁移恢复使用明确的原生历史文件注册新索引，不混入其他账号的历史或 SQLite 数据库。
- 首轮完整后台回归 295 项通过；补充实际主进程 IPC 的内存窗口替身后，账号迁移、并发保存、失败关闭、旧连接编号、原生历史、错误密码、跨窗口切换、独立布局、迟到请求与登录中关闭等相关 17 项通过。窗口替身没有启动 Electron 或浏览器，不能代替真实桌面验收。
- 已合并远端 `e31ae42` 的 Windows CLI 路径识别与网络连接入口改进；合并后的 TypeScript 与隔离构建通过。当前正在使用的用户端和管理端未重启，本机旧数据未迁移；重新运行新版用户端时才执行迁移。
- 最终合并版本完整后台回归 300 项全部通过；迁移发布前补充设置格式校验后，实际主进程窗口路由与迁移相关 6 项复测通过，TypeScript 和隔离构建再次通过。待提交文件敏感信息与 diff 空白检查通过，测试日志和本机诊断未纳入提交。

# 2026-09-24 代理独立监控与 CLI 异常状态

- 管理端网络出口改为独立 utility process，私密配置只通过父子 IPC 传递；CPU、驻留内存、隧道数、在线成员、累计流量和最近两分钟速率每 2 秒采样。构建将代理入口放入不可变资源目录，保留已运行版本使用的入口。关闭出口、重新配置、进程异常退出及迟到回复均有资源回收与状态处理。
- CLI 结构化错误和诊断输出进入会话连接状态，工作会话与成果整理页面显示重连次数、HTTP 401/403/429/5xx 等错误；收到服务输出后恢复。后台清理保留最终失败原因，不将 403 简单归类成登录失效。用户端向出口仅上报白名单状态字段，每 30 秒补报，管理端标记超过 90 秒未更新的状态。采样超过 10 秒未更新时不再将旧速率和资源读数显示为实时数据。
- 修复代理端连接回收：客户端断开、上游握手卡住或拒绝后释放两侧连接；上游 HTTP 403 的具体错误码保留在连接记录中。首包剩余数据参与统计，连接检测与 CLI 状态上报不计入模型流量；累计值不受最近 100 条记录上限影响。
- 完整后台回归 288 项通过。随后增加实际 Node 子进程运行生产代理入口的资源/IPC 验证与过期显示用例，连同相关渲染、账号状态、进程回收和不可变构建用例共 15 项复测通过。子进程验证最初在测试中调用构建，被后台保护器拦截，已改用现有 TypeScript 测试加载器且继续加载后台保护器，没有放宽保护规则。最终 TypeScript 和隔离构建通过。
- 最后补充保留 JSON-RPC 结构化 HTTP 错误码及无文字错误对象，防止被通用错误文本覆盖或误认为成功；CLI、厂商登录和运行时相关 15 项复测通过，错误分类单独回归通过。
- 本次未启动 Electron、浏览器或桌面测试，未重启当前正在为成员提供出口的管理端；独立 utility process 的 Electron 桌面集成与实际界面仍需任务结束后验收。更新功能需两端获取新代码后重新启动；旧用户端可继续使用隧道，但没有 CLI 状态上报。本机资源采样、地址与诊断日志只保留在被 Git 忽略的目录中。

# 2026-09-24 提交前回归与远端合并

- 完整后台回归运行 277 项：首轮 274 项通过，3 项旧测试因缺少当前账号或完整绑定被新增的账号归属校验拦截。补齐权限切换、提交锁定测试夹具，保留生产校验；相关 19 项全部通过，其余 258 项首轮通过。
- 合并远端 `81e9120` 的管理端服务器环境说明改进，双方部署文档改动自动合并。合并后 TypeScript、隔离构建、管理端与账号隔离 19 项后台回归通过，构建未覆盖运行中的应用文件。
- Python 服务端测试共 93 项，89 项通过，4 项按平台条件跳过。首轮有 1 项在 Windows 临时目录替换 JSON 时遇到 `WinError 5`；未修改代码，单独重新运行完整服务端测试后通过。保留此环境偶发失败记录，不据此认定原因已解决。
- diff 空白及待提交文件敏感信息扫描通过。本次未启动 Electron、浏览器或桌面测试；界面布局及启动窗口仍需实际桌面验收。本机黑屏诊断、账号记录、测试日志和生成物未纳入提交。

# 2026-09-24 应用已启动但主窗口被隐藏

- 最近一次管理端、用户端启动日志均成功，两个 Electron 主进程存在，但 Win32 枚举发现两者的 `Chrome_WidgetWin_1` 主窗口均不可见。根因是上一轮新增的 `windowsHide: true` 向应用传递了隐藏首个窗口的启动参数，覆盖了 Electron 的正常显示请求。
- 改为 `windowsHide: false`，继续使用独立进程及不继承标准输入输出释放启动终端。控制台测试替身仍单独设为隐藏，避免测试时弹出终端；生产应用与测试替身的显示要求明确区分。
- 使用本机 `pythonw.exe` 作为无界面的 GUI 子系统进程，实际通过生产启动函数读取 Win32 `STARTUPINFO` 和控制台关联：旧参数 `forcesHiddenFirstWindow=true`，修正后为 `false`；两种情况下均无控制台关联。该检查未创建窗口或启动 Electron。3 项启动相关后台测试、TypeScript 与 diff 检查通过。
- 验证既有进程路径、PID、主窗口类名和标题后，仅对两个被隐藏的工作台主窗口调用恢复显示；再次查询两者 `visible=true`。未关闭进程、重启应用、重装依赖或修改用户数据。

# 2026-09-24 启动后残留空白终端

- 枚举本机可见窗口确认存在标题为 `powershell.exe` 的 Windows Terminal 窗口；该次启动的 PowerShell 已退出，对应 Electron 与控制台宿主仍存在。原流程从 PowerShell 直接启动 Electron，未断开控制台句柄。
- 新增 `launch-desktop.mjs`，用独立进程且不继承标准输入输出的方式释放启动终端，收到启动确认后解除父进程引用。初版的 `windowsHide: true` 已在上方“主窗口被隐藏”修复中更正为 `false`。路径通过独立参数传递；清除子进程的 `ELECTRON_RUN_AS_NODE`，不改父环境。两版共用，启动错误仍交给原日志与提示流程。
- 新增 2 项无进程替身测试通过，覆盖启动方式、含空格/中文/符号的路径、环境、启动确认与错误；启动入口源码检查通过。首轮发现测试加载器不支持该文件的顶层 await，改为 Promise 回调后复测通过。其余 16 项账号隔离、启动依赖与入口检查通过；PowerShell 语法/BOM、类型检查、隔离构建通过。已更新桌面启动测试夹具，本轮未执行真实应用启动或关闭任何现有终端，Windows Terminal 的实际消失效果尚未目视验收。

# 2026-09-24 成果库原位展开与多窗口排查

- 团队和个人成果库改为全宽紧凑条目，不再保留右侧详情栏。按最新反馈，操作按钮移到标题/摘要右侧并排排列，减少条目高度；默认收起，点击标题后完整内容在下方占整行，一次展开一条。操作栏在收起时可用，勾选和操作按钮独立于展开按钮；编辑也在条目内进行。动态单条结果页仍只显示该成果。
- 区分定时后台刷新与手动刷新/保存：定时刷新期间仍阻止提交旧版本，但“存入个人成果库”按钮保留原颜色，不反复变暗。正在存入、个人库已有、编辑及批量选择等其他禁用状态仍正常显示。
- 存入个人库成功后立即更新本地比对状态并隐藏一致条目；操作另一条成果不会收起当前展开项。保留版本差异、删除个人副本后重新出现、历史仍视为保留、权限及批量确认逻辑。更新了桌面回归的选择器和右侧按钮/下方正文位置用例，但未运行桌面测试。
- 35 项后台测试通过，包含原位展开结构、操作栏位置、无重复正文/按钮、标签、成果增删、历史、批量删除与角色权限、团队/个人差异和动态结果。TypeScript 与隔离构建通过；未覆盖运行中的应用文件，未启动 Electron 或浏览器，未做实际显示尺寸目视验收。
- 只读排查多窗口数据确认：原账号的工作会话仍在 `TeamAgentUser/instances/2/sessions.json`，对应会话目录及事件文件存在；两个窗口最近登录的账号不同。当前账号隔离只筛选本窗口数据，不跨窗口合并；数据未删除，本轮未搬移或修改用户记录。这是先前仅覆盖同一 Store 切换账号的测试未覆盖的使用场景，已补充账号文档。具体账号、项目和诊断记录仅留在本机。

# 2026-09-24 启动前置窗口样式

- 用户端、管理端共用浅色和绿色点缀的启动窗口，统一标题与版本标识、三阶段进度、当前步骤说明、单步/总耗时、日志与取消按钮。环境检查及按需安装归入第一阶段，构建和打开分别高亮第二、三阶段；已完成阶段显示勾选，不使用虚构百分比。支持 DPI 缩放和 Esc 取消。
- PowerShell 语法、UTF-8 BOM 和 diff 空白检查通过；已有 11 项启动入口/依赖后台测试通过。用内存中的控件替身执行实际阶段更新函数，三阶段状态、耗时、日志记录及取消均通过；没有创建窗口、重装依赖或启动工作台。未做实际桌面显示与高 DPI 目视验收。

# 2026-09-24 日志入口与切换账号后的数据隔离

- “查看日志”改为用记事本直接打开本次启动的日志文件，不再打开日志目录；PowerShell 语法与 UTF-8 BOM 检查通过，未在本机启动记事本或桌面进度窗口。
- 只读检查本机记录确认：当前验证账号与界面中旧会话的绑定账号不同。原快照把全部账号的会话/输入/整理/传输交给界面，左栏只按工作组筛选，导致同组旧会话混入。
- 现在快照按服务器/端口/账号/指纹过滤，按 ID 的会话、草稿和个人成果操作也验证归属。切换时隐藏过渡数据，提交成功后重新创建账号视图以清除选择、预览、输入缓存和弹窗；旧异步返回被拒绝。未完成的写入、运行中的会话/整理/传输会阻止切换，上传缓存清理只作用于当前账号。
- 重启恢复整理成果时使用来源草稿的账号；手工成果和个人合并结果记录明确归属。旧个人成果补齐归属放在已有修复备份之后，保留原始备份内容。原账号记录保留在本机，登录失败和断网使用最近一次验证成功的身份。
- `account-isolation` 新增 5 项后台回归：同组同项目多账号、历史与草稿/传输/输入、旧 ID 访问、服务器/端口/指纹、切回与断网、登录中间态、迟到请求、运行/写入期间切换、重启成果归属、上传缓存独立性。对本机 JSON 只读执行同一过滤函数，新账号的会话/整理/传输均为空，原账号视图仍保留旧会话；未写本机用户数据。
- 相关回归分批执行：首批 27 项、扩展批 25 项通过；第三批 38 项发现 2 处兼容问题（测试夹具缺少完整账号绑定、旧备份提前补齐了归属），修正后 15 项相关用例复测全部通过，其余 36 项先前通过。覆盖会话设置/引导、文件、项目目录、成果增删与历史、整理生命周期/权限、账号同步、附件及启动依赖。类型检查和隔离构建通过，未运行 Electron、真实模型 CLI 或桌面交互测试。

# 2026-09-24 管理端重复安装依赖

- 本机 09:11 管理端日志显示 `npm ci` 安装了 445 个包（25 秒）。此前启动器按两个 JSON 文件的完整字节计算安装记录；近期变更只有项目描述、测试命令和根目录 Node 版本要求，锁定依赖未变，仍会被误判为需要重装。
- 改为依赖字段、锁定的包和安装脚本的规范化指纹，并核对 npm 安装记录与包版本。描述、测试命令、根版本/engines、JSON 键顺序或换行不触发重装；旧启动记录和手动/离线安装经核对后可复用。依赖变化、缺包、已记录的 Node 主版本变化仍触发准备，安装中断保留重试标记。进度和日志显示具体原因。
- 11 项后台测试通过，覆盖元数据更新、旧记录迁移、手动安装、实际依赖变化、缺包、锁文件不一致、Node/架构变化和中断重试；5 项纯 PowerShell 进程替身检查、TypeScript、启动器语法/BOM 与 diff 空白检查通过。
- 对本机目录只读运行新的依赖检查，结果为 `install: false`，现有依赖可以复用。本轮没有执行 npm 安装、下载、启动 Electron 或重启当前应用；已更新桌面启动器测试夹具，但未执行桌面集成测试。这些检查不替代新电脑实际安装验收。

# 2026-09-23 安装说明与提交前验证

- 安装说明进一步改为显式命令流程：源码目录检查、带终端输出的 `npm.cmd ci`、Electron 下载与文件检查、用户端/管理端启动；每步写明成功标志、失败处理、日常启动与更新方式。双击入口作为可选方式，明确手动安装与启动器准备记录的差异。
- 参考 GitHub Desktop 的 `docs/contributing/setup.md`（依赖、构建、启动及环境验证）和 Cherry Studio 的 `docs/contrib/development.md`（明确的安装与启动命令）。按本仓库的 npm 脚本和 Electron 安装脚本编写，未照搬其他项目的包管理器或环境要求。
- 本次文档修订核对了本机脚本入口及已安装的 Electron 文件，15 个 PowerShell 命令块语法解析通过、56 个本地文档链接与相关安装锚点检查通过，`git diff --check` 通过；没有重装依赖、执行真实下载或启动客户端，这些检查不代替新电脑安装验收。

- 参考 PowerToys 和 GitHub Desktop 的 README 组织方式，将获取源码、安装 Node、选择客户端、首次连接、更新和排障置于 README 前部；明确源码 ZIP、另行交付的安装包及本机 release 输出目录的区别。核查时 GitHub Releases 尚无发布包。
- 提交前复验 32 项后台测试（项目目录、成果差异与上传来源、类别筛选、界面静态渲染、不可变构建资源和连接切换），全部通过；另有 5 项启动进程替身测试、类型检查与隔离构建通过。
- 检查局限：以往启动器集成测试以本机 Node 和模拟 npm/Electron 流程为主，不能验证陌生电脑的真实依赖下载、代理、企业证书或桌面进度窗口；原流程缺少超时，模拟成功/失败退出并未覆盖无限等待。新增替身测试覆盖超时与取消，但本轮没有在新 Windows 环境执行真实安装，也未复现报告机器的卡住原因。不能将代码检查通过表述为新电脑安装已验收。
- 合入远端 `809f635` 及其之前的 Claude Code 和手册更新；保留三种提供方的选择、登录检测与权限逻辑，同时保留按项目目录、成果差异和类别筛选。Markdown 手册同步当前交互，原 v1.3 网页、截图、Word/PDF/ZIP 保留并标明版本边界，没有重新生成桌面截图或离线包。
- 合并后扩展复验至 43 项后台测试（含 Claude 模拟协议、资料同步与附件），全部通过；类型检查与隔离构建再次通过，未覆盖正在运行的应用文件。

# 2026-09-23 源码启动等待与前置条件

- README 顶部集中列出源码/安装包、本机权限与 PowerShell、Node/npm、下载网络、Codex/Cursor 运行文件与认证、团队账号、Linux 服务端、可选模型出口，以及启动日志和诊断命令。明确源码下载与模型代理为独立网络阶段。
- Node 最低版本统一为 x64 22.12.0，与锁定的 Electron 下载依赖一致；package.json、锁文件根元数据、启动器、README 和部署指导保持一致。已有 Node.js 24.21.0 可复用。
- 原启动器将 npm 安装、Electron 下载和构建输出重定向后无限等待。现在显示 WinForms 启动进度、步骤耗时、日志入口与取消；安装/下载分别限时 10 分钟，构建 3 分钟，排队 15 分钟，运行时自检 10 秒。取消和超时仅停止本次创建的子进程树，日志记录阶段与失败原因。
- `tests/startup-process.test.ps1` 使用纯进程替身，5 项通过：成功、非零退出、超时清理、执行中取消和启动前取消。未创建真实进程、窗口或网络请求。`tests/launcher-source.test.ts` 后台检查通过；PowerShell 语法检查、TypeScript、`node scripts/build.mjs --check` 与 diff 空白检查通过。
- 已更新完整启动器测试以包含新的进程辅助文件，但按本机不做桌面测试的约束，本轮未执行真实启动、WinForms 交互、依赖下载或完整桌面启动器测试，未重启当前应用。对方电脑的具体阻塞阶段仍待该机启动日志确认；修复的是已从源码确认的无限等待和无进度问题。

# 2026-09-23 统一成果类别筛选与说明排版

- 团队和个人页共用“全部类别”控件，只统计当前成果列表中实际标题的【类别】，支持自定义、多类别和未分类；不读取项目预设类别作为筛选候选。选项附带数量，不随关键词搜索消失。个人页显示原有类别与本地别名，不再用预设类别覆盖标题中的自定义类别。
- “维护全部成果 / 返回差异列表”改成管理员勾选项“包含个人库已有成果”，只切换展示范围并清空旧筛选、多选，不修改成果。团队库混有不同内容形式时单独显示“全部内容形式”，只有文字成果时不展示这一控件。
- 使用说明改为短标题与释义对齐的规则列表，分开列出存入、删除副本、团队更新和查看全部；维护行为另放底部提示。个人页切换类别或搜索时清空隐藏勾选，筛选无结果与空库使用不同提示。
- 后台测试 `tests/result-labels.test.ts`、`tests/materials-rendering.test.ts` 共 8 项通过；验证库内自定义类别、多类别计数、元数据与标题不一致时按实际标题筛选，以及空库不出现预设类别。TypeScript 与用户版/管理员版构建通过。
- 鉴于本机此前短暂黑屏，本轮未启动 Electron 界面测试或重启工作台；已同步更新界面回归脚本的名称与勾选操作，未声称完成本轮桌面交互复测。

# 2026-09-23 团队可见范围与标题标签筛选

- 团队成果页取消操作状态筛选和待整理/已整理标记，“整理 / 编辑”改为“编辑成果”，保存按钮统一“保存修改”。团队表示项目共享范围，个人副本差异规则和既有服务端操作权限保留。
- 普通成员和管理员均按标题开头实际存在的【标签】筛选，支持自定义、多标签、无标签及数量；不按固定类别重写标题。搜索、类型、标签变化会清空批量删除与合并勾选；当前标签变为零条时保留零条筛选，避免自动扩大操作范围。
- `node scripts/test-background.mjs tests/result-labels.test.ts tests/materials-rendering.test.ts`：7 项通过，覆盖标签解析、数量、别名不改标签、零条筛选和双方角色渲染。
- `node scripts/team-result-difference-smoke.mjs`：通过。双方角色均显示全部 6 条，验证自定义/多标签/无标签筛选、切换清空勾选、编辑标签后自动更新、零条结果；导入、删除后重现、远端更新、个人改写保留、账号切换和旧上传关联回归通过。截图位于 `.test-data/team-result-difference-1790145234972/`。使用隐藏窗口、关闭硬件加速、隔离数据和模拟远端，未启动真实模型或访问团队服务器。
- 用户报告再次短暂黑屏后停止启动界面测试；后续仅执行文件检查、TypeScript 与构建。本轮未重新运行完整协作界面脚本。
- `npx tsc --noEmit`、`node scripts/build.mjs`、`git -c core.safecrlf=false diff --check` 通过。用户版和管理员版构建已更新，当前运行中的工作台未重启。

# 2026-09-23 统一团队成果数量与精简页面说明

- 本机两个账号保存的团队目录均为同样的 6 条。该项目的组管理员 `l00851001_test` 被旧界面默认筛选为“未操作”，显示 5 条；普通成员 `l00851001` 不受此筛选限制，显示 6 条。差的 1 条是已操作的 v29 Attention V 综合整理结果，来源不是两个账号收到不同的团队目录。
- 两种角色默认均显示全部操作状态，管理员可明确筛选；数量显示完整范围，筛选时显示“显示 N / 共 M 条”，提供“清除筛选”。切换账号、项目或差异/维护视图会重置状态筛选，默认范围不再受角色影响。
- 删除页内分散的长介绍与维护提示框，统一为范围/数量、紧凑操作栏和筛选栏；详细规则移入默认折叠的“使用说明”，支持 Escape 收起。
- TypeScript、4 项渲染回归、两版构建及 `git diff --check` 通过。`team-result-difference-smoke.mjs` 在隐藏窗口、独立数据、模拟共享区中验证两角色同为 6 条，5/6 与 1/6 主动筛选、搜索及清除、说明展开收起，并复验既有存入/删除/版本/上传关联流程。已检查成员和管理员页面截图。
- 完成时本机仅管理端在运行，用户端已退出；下次打开用户端会加载最新构建。本轮未访问生产共享区或执行真实 CLI。

# 2026-09-23 修复个人整理后上传的成果重复显示

- 本机记录确认“提交必须按NVFP4到HiF4的六接口执行”的个人条目来源为本地整理，上传记录、个人正文及另一账号保存的团队正文完全一致。旧差异比对只识别远端来源 ID，遗漏了本地整理到上传文件的对应关系，因而误判未存入。
- 上传任务保存原整理来源，成功后将唯一上传路径、初始修订和文件校验值关联到个人成果；启动时兼容旧整理及完成的上传记录。比对与重复存入都识别该关联，保留原个人条目；团队更新、个人改写与删除仍按差异规则处理。关联随个人成果同步，不依赖整理任务永久保留，不按相同标题合并其他来源。
- 对本机已有记录进行了只读复现：修复前为 `missing`，内存补齐关联后为一致；没有修改运行中客户端的数据文件。19 项后台回归通过，覆盖旧数据迁移、单成果旧草稿、新上传、重启和同步、其他账号/项目隔离、失败上传、历史/删除/改写与不同来源。
- 隐藏 Electron 界面回归通过，新增“本地整理上传后隐藏、删除个人副本后重新出现”流程；使用独立测试数据和模拟共享区，没有执行真实 CLI。
- TypeScript、两版构建和 `git diff --check` 通过；开发构建已更新，用户端重启后自动补齐已有上传关联。
- 后续检查发现本机仍运行 11:17 的旧进程，其中一个账号窗口处于隐藏状态。确认各账号无运行中的会话、上传或整理任务后，逐个通过正常关闭请求退出旧窗口，未强制结束进程；12:32 重开两个账号窗口并确认可见、响应正常。启动已将上述个人成果的上传关联自动保存，个人正文仍保留。团队登录需由用户重新完成，尚未在重新登录后的真实团队列表上复核。

# 2026-09-23 团队成果与个人库差异

- 团队页默认按共享来源、修订、文件校验值和正文展示差异，每条可直接“存入个人成果库”；一致副本隐藏，个人删除后重新出现，历史仍算保留。动态处理状态与差异独立，管理员可切换“维护全部成果”，单条动态详情仍能打开已存成果。
- 团队删除不删除个人副本和冻结的会话引用；团队更新需明确存入。个人改写保留，另存团队版本后重复存入复用已有副本。导入串行处理并校验账号、远端版本；导入与个人删除保存失败时回滚变更，避免显示虚假的成功状态。
- TypeScript、两版构建、`git diff --check` 通过。后台差异、动态、渲染共 18 项回归通过；另运行已有个人成果删除、批量删除、别名 3 项回归，共 21 项通过。
- `node scripts/team-result-difference-smoke.mjs` 使用生产组件与 Workbench 方法、模拟远端目录、独立数据、隐藏窗口和禁用 GPU，验证存入失败/成功、个人页删除后重新出现、当前页状态刷新、历史保留、个人改写、团队更新、管理员维护、动态详情、团队删除和界面切换。最终通过，无页面异常；未运行真实 CLI 或访问生产共享区。
- 开发构建已更新，运行中的用户端需重启后生效。差异规则及状态表已写入用户指南。

# 2026-09-23 收起会话文件入口

- “会话文件”从会话顶部的独立按钮收进右上角“⋯”菜单；点击后打开原文件列表，文件链接仍可直接预览。
- 类型检查、两版构建通过。使用独立数据、隐藏窗口和禁用 GPU 实际检查默认隐藏、菜单打开文件列表、Escape 关闭、点击外部关闭；没有调用真实 CLI。用户指南与已有界面检查脚本已同步。
- 开发构建已更新；正在运行的用户端固定使用启动时的配套界面，需要重启后显示本次布局。

# 2026-09-23 修复新增项目目录接口的运行版本混用

- 本机旧用户端进程在 10:58 启动，11:10 的构建更新了磁盘界面资源；新增账号窗口加载新界面，却仍调用旧主进程，因此“暂不设置”返回 `未知操作：project.directory.save`。源码和磁盘主进程均已包含接口，根因是运行版本混用。
- 用户版和管理版的页面、样式、脚本与 preload 按内容哈希保存，主进程固定加载本次构建对应的资源。重复构建保留旧资源，全部资源准备完成后才原子替换主入口，避免刷新或新增窗口拼接不同版本。
- TypeScript 检查、两版构建和 7 项后台回归通过。`node scripts/project-directory-smoke.mjs` 使用独立测试数据、隐藏窗口和禁用 GPU，验证生产 IPC 的“暂不设置”、重启记忆、登录/会话无目录输入，以及模拟更新后刷新与新增账号窗口仍使用原配套页面；测试账号配置不存在的 CLI 路径，不执行真实 CLI。
- 检查本机用户端没有运行中的任务后，通过正常窗口关闭流程退出旧进程并重新打开两个账号窗口；新用户端进程已启动并响应，需重新登录团队账号。本轮已更新开发构建，未生成安装包。

# 2026-09-23 项目独立的本机代码目录

- 登录不再填写代码目录；首次进入项目时可填写或明确跳过，两种结果均持久保存。后续从“设置 → 项目设置”修改，各账号、各项目独立，普通成员也可修改自己的本机目录。
- 新建会话不再选择“绑定共享项目”或重新填写目录，直接使用当前项目及其保存的目录；留空时使用独立调研目录，不沿用其他项目的上次路径。修改项目目录不改动已有会话。旧的项目路径按已验证账号迁移，设置页的过期快照不会覆盖新路径。
- TypeScript 检查通过；`node scripts/test-background.mjs tests/project-directory.test.ts tests/workspace.test.ts tests/provider-connection.test.ts` 共 10 项通过，覆盖空路径登录、按项目/账号隔离、跳过与重启、旧路径迁移、保存失败回滚、会话目录保持、普通成员编辑和连接检测。
- 已同步用户说明与现有桌面检查脚本；本轮未构建、启动或重启客户端，未运行桌面检查脚本或真实 CLI。

# 2026-09-23 成果入口、整理可读性与会话网络设置

- 项目成果库合并为一个导航入口，页内区分团队和个人；成果整理支持批量删除，已确认无新增内容的任务不再计入待整理。
- 新建及已有会话均可配置当前账号窗口共用的管理端出口，并重新检测连接、CLI 登录和模型信息；运行中的任务阻止切换出口，迟到检测结果不会覆盖新上下文。
- 整理、合并及交接记录共用可读性要求；整理结果默认排版展示，技术追溯信息收进来源详情，列表摘要保留完整语句。
- TypeScript 检查及 `git diff --check` 通过。`node scripts/test-background.mjs tests/provider-connection.test.ts tests/result-reading.test.ts tests/materials-rendering.test.ts` 的 13 项后台回归通过，覆盖模拟连接检测、结果可读性与静态组件渲染。
- 批量删除及待整理计数的生命周期回归已补充，但本轮未运行相关完整测试。未构建、重启客户端、运行桌面界面测试或验证真实 Codex/Cursor 连接；上述检查不代表桌面端或真实服务验收。

# 2026-09-21 双击启动时 Electron 运行文件缺失

- 实际启动日志显示 `npm ci` 成功安装 445 个包，但 Electron 44.3.0 不再通过 npm 安装后脚本准备运行文件。旧启动器直接检查 `electron.exe` 并报错，绕过了 Electron 的首次使用下载；本机 `ignore-scripts=false`，并非弹窗所推测的禁用脚本。[Electron 官方安装说明](https://www.electronjs.org/docs/latest/tutorial/installation#binary-download-step)明确区分 npm 包安装与运行文件下载。
- 两个入口共用的隐藏启动器增加独立运行文件准备，检查程序、版本和路径记录，使用当前已锁定版本自带的安装程序与校验和。npm 安装成功即保存依赖状态，运行文件失败后只重试该步骤；不重复清空依赖、不启动旧构建。弹出错误前释放准备锁，避免一个错误窗口持续阻塞另一个入口。
- 本机还复现公司代理证书导致 `SELF_SIGNED_CERT_IN_CHAIN`；同一地址使用 `--use-system-ca` 后返回 200。启动器在 Node 支持时仅为 Electron 安装进程启用系统证书，保留 TLS 校验，不修改全局 npm/Node 设置。本地已有同版本 ZIP，经包内 SHA-256 校验后写入官方缓存，补齐运行文件。
- 真实用户版启动路径已验证：启动器完成 `npm ci`、Electron 准备和两版构建，进程退出码为 0，用户版“团队工作台”窗口已打开且响应正常。日志：`.test-data/launcher/user-20260921-181941-085-33036.log`；Electron 与构建错误日志均为空。仅清理先前两个失败/等待中的启动器，没有终止工作会话。
- 启动回归共 12 项：11 项整组通过，文件缺失/版本修复用例处理 Windows 可执行文件退出后的短暂占用后，单独复验通过。覆盖 Electron 按需安装、下载失败重试不重装依赖、缺失程序/路径记录/版本不符的恢复、安装程序假成功、两版并发、带空格及中文路径、便携 Node 回退、构建失败阻止启动。TypeScript 检查、真实构建和 `git diff --check` 通过。
- README 与部署指导已补充独立下载、缓存重试、企业证书和日志位置。未生成新安装包；已有源码启动入口直接生效。

# 2026-09-21 用户端与管理端状态转换扩展检查

- [状态图与操作边界](docs/workflow-states.md) 扩展至 13 个部分，覆盖连接与离线工作、CLI 登录、会话、授权、模型/Skill、成果整理、上传、派活、文件查看、网络出口、空间统计和管理端恢复；每类列出用户可见信息、允许操作及回归入口。
- 修复登录检测阶段停止后仍可能发送的问题，Codex/Cursor 均验证。停止保留原生会话、记录、输入和引用，显示“已停止”；正常退出或重启识别未完成任务为中断，既有错误不再被改成空闲。关闭会话后拒绝修改权限和项目说明，界面禁用更新入口。退出因保存失败被阻止后恢复保存能力。
- 整理任务删除期间拒绝重试和重复删除，清理历次辅助会话；缺少旧上传标记的已保存合并结果仍不可删除或重新生成。派活先准备本地会话与引用，再核对当前修订并转为进行中；本地准备失败或任务已取消，不留下可继续工作的半成品会话。
- 传输队列记录未保存成功前不开始上传。入队、运行及完成阶段的保存失败均可恢复，不使队列卡死；重试清零进度、复用原目标，保留远端幂等边界。出口检测失败清除旧可用状态，关闭或换配置后的迟到检测不能恢复旧结果。
- 文件预览、空间统计、模型与额度读取增加切换目标的过期结果保护。文件不存在时清除旧预览和打开按钮，不同时显示“没有记录”；模型未登录时清除读取中与旧账号结果。
- 全量 Node 测试 **159/159 通过**；Python 测试共 **83 项，79 项通过、4 项环境相关跳过**。TypeScript 检查、用户版/管理版构建及 `git diff --check` 通过。新增 `workflow-states.test.ts` 的 8 项回归，并补充派活竞态和出口状态测试；自动保存失败后的关闭恢复由既有 `usability` 用例复验。
- `npm run test:workflow-ui` 通过：直接渲染生产组件，控制迟到成功/失败的返回顺序，验证空间统计切换、文件切换/缺失、模型及额度切换。截图：`.test-data/workflow-states-1789984902375/`。启动等待已明确等待测试页面就绪，避免页面尚未加载时读取测试 API。
- `npm run test:storage-ui` 通过：管理端真实页面的项目组、成员和文件夹统计及导航。截图：`.test-data/admin-storage-1789984771905/`。
- `npm run test:session-ui` 通过：两种 CLI 的模型、整理、授权、取消/重试、关闭/重开及轨迹上传；补验运行中停止显示“已停止”、继续发送清除停止状态，以及关闭会话禁用项目说明更新。截图：`.test-data/session-experience-1789984883721/`。
- 使用独立测试数据、本地协议替身和隔离共享区，没有停止用户现有会话、修改真实团队数据或发起真实模型请求。已更新开发构建，未生成安装包。

# 2026-09-21 旧会话引用去重与按要求处理个人结论

- 已同步远端 `633475c`，保留源码启动器、服务器环境准备与恢复等更新；本轮修改叠加其上，用户指南同时保留双方内容。
- 旧会话可能为同一来源、同一哈希保存多个引用 ID。展示与实际发送统一去重，勾选任一副本仍有效，取消时清除该内容的重复选择；任一副本被当前 AI 对话接收后只显示一份已带入资料。原消息、快照和文件不改写，不同来源或不同内容不合并。再次加入旧版共享成果复用已有引用，新修订仍独立保存。
- 顶部“AI 参考内容”显示已带入数量；发送后输入框上方标签消失表示已被接收，不表示资料被删除。
- 个人结论直接勾选后才出现处理按钮；可选择 1 至 20 条，再填写处理要求并生成预处理结果。支持单条改写、提炼、对比、行动建议和合并。后端提示词同步取消必须融合为统一结论的限制，任务状态与结果标题、编辑区、通知使用处理/预处理描述。确认保存前原结论保持使用中，保存时校验版本并将所选原结论移入历史。
- 合并远端后的全量 Node 测试 **150/150 通过**。Python 测试共 **83 项，79 项通过、4 项环境相关跳过**。TypeScript 检查、用户版/管理版构建和 `git diff --check` 通过。
- 核心回归覆盖旧重复引用、非首个副本勾选、失败重试、接收状态共享、新原生上下文重发、同名不同来源/版本保留、原始历史不改写；单条结论处理验证实际 CLI 请求遵循人工要求，预览不归档，手工修改后确认保存。
- `node scripts/session-context-smoke.mjs` 通过：Codex/Cursor 接收状态、旧重复引用只显示/发送一次、勾选/取消、重启保留；个人结论先选后处理、切换范围清空、返回选择不建任务、自定义方向、处理中/待确认状态、编辑结果、明确保存与重启。截图：`.test-data/session-context-ui-1789982885504/`。
- `node --import tsx scripts/activity-results-smoke.ts` 通过，复验个人结论卡片增加处理勾选后，别名、动态处理记录、保留/删除选择及批量删除仍正确。截图：`.test-data/activity-results-1789982706370/`。
- `node scripts/server-environment-smoke.mjs` 通过，验证同步的远端环境检查、在线/离线准备、失败重试与恢复流程。截图：`.test-data/server-environment-1789982885531/`。
- 测试使用独立数据与 CLI 替身；真实 Linear 会话仅用于读取问题证据，未停止任务、修改消息或清理快照。已更新开发构建，未生成安装包或修改真实服务器。

# 2026-09-21 状态流程检查、个人处理记录与会话引用展示

- 状态图和编辑/删除操作表见 [用户工作流状态与操作边界](docs/workflow-states.md)，覆盖团队动态、个人结论、会话引用、整理/上传和派活。用户指南已同步说明。
- 输入框上方仅展示尚未被当前 AI 对话接收的资料；成功接收后收进“AI 参考内容”并标记“已带入”。发送失败继续保留待带入状态，恢复同一对话不重复发送。显示与发送逻辑共用接收记录，按原生对话和文件哈希判断。
- 动态加入个人结论库只写本机个人副本；动态记录处理时间、目标结论/会话的当时名称和采用版本。从个人结论再加入会话也回写来源动态；重复操作去重，旧快照不处理新版本或删除通知。
- 动态明确区分待处理、历史和全部；自己操作自动归档有原因，旧记录不伪造处理方式。共享移除保留已处理历史并提示原成果不可用；保留/删除个人副本有独立处理记录。删除动态仅清理本机记录。
- 同一共享成果版本并发加入同一会话复用快照；关闭会话前后（包括远端读取期间关闭）均拒绝新增引用，失败不记录成功。已保存合并任务明确显示不可删除。
- TypeScript 检查、用户版/管理版构建和 `git diff --check` 通过。全量 Node 测试 **137/137 通过**；新增生命周期回归覆盖个人副本隔离、操作目标与版本、幂等、关闭会话竞态、旧快照、新修订、共享删除和重启持久化。既有回归继续覆盖编辑、归档/恢复、批量删除、取消/重试、上传冻结和派活角色边界。
- `node scripts/session-context-smoke.mjs` 通过：Codex/Cursor 均覆盖结论推荐、带入后发送失败、重试成功、顶部已带入列表、输入框清理、后续真实协议去重、旧会话迁移和重启状态。截图位于 `.test-data/session-context-ui-1789981207693/`。
- `node --import tsx scripts/activity-results-smoke.ts` 通过：个人库归属、历史不混入待处理、处理目标展示、重复导入不重复记账、别名、来源移除后处理记录保留、保留/删除选择和批量删除持久化。截图位于 `.test-data/activity-results-1789981218451/`。
- `node --import tsx scripts/collaboration-smoke.ts` 通过：多人账号窗口、共享文档整理、已保存任务保护标记、个人结论加入多个会话、项目资料及重启保留。截图位于 `.test-data/collaboration-ui-1789981550449/`。
- 使用隔离本地共享区、独立测试数据及 CLI 测试替身；未改变真实团队内容。本轮更新开发构建，不生成安装包。服务端协议无新增变更。

# 2026-09-21 指定成员派活与会话文件查看

- 子管理员可给本组指定成员派发任务，附任务说明、验收要求和最多 20 条共享结论的版本快照；普通成员只查看自己的任务。成员开始任务时创建带引用与输入草稿的会话，继续任务复用该会话，发送前不调用模型。任务支持开始、完成和取消，内容修改通过取消后重新派发。
- 任务候选结论按 BM25 相关性排序；服务端检查实时成员身份、管理员权限、项目归属和引用版本。关联结论从共享区移除后，已派发任务与会话仍保留引用快照。
- 会话中的本地文件链接可打开预览，工具栏“会话文件”列出已记录或对话中提及且实际存在的文件，显示完整路径，支持复制路径、打开所在文件夹和使用系统应用打开文档。缺失文件明确提示；程序与脚本仅预览或定位。没有路径、也没有工具文件记录的产物无法自动识别。
- Linux 管理端新增“更新服务端功能”，经明确确认后更新内容服务，保留成员连接与已有数据。旧服务端在调用任务功能时给出升级提示；本地模拟模式无需升级。管理员 SSH 内置程序改用输入流传送，避免程序增长触及命令长度限制。
- TypeScript 检查、用户版和管理版构建通过。全量 Node 测试 136/136 通过；最终 Python 测试共 49 项，48 项通过、1 项 Linux 实际 ACL 测试因环境跳过。
- `node --import tsx scripts/assignments-files-smoke.ts` 通过，覆盖派活、相关结论选择、成员待办、新建与继续会话、双方完成状态、历史文件链接预览与完整路径、重启持久化。截图位于 `.test-data/assignments-files-1789977219133/`。
- `node scripts/server-upgrade-smoke.mjs` 通过，覆盖 SSH 输入流传送、取消时不升级和确认后执行一次升级。截图位于 `.test-data/server-upgrade-1789978069914/`。
- 使用隔离测试数据与回环 SSH 服务，未操作真实团队数据或升级真实服务器。已更新 `dist/user` 和 `dist/admin` 开发构建，本轮未生成安装包；本次源码提交包含上述功能，旧提交 `fa4105958de5c6cef9409f8895feae5ec3030b6a` 不包含这些功能。另一台电脑需拉取包含本次改动的版本后重新构建。

# 2026-09-21 动态与本地结论批量删除

- 动态和项目结论支持多选、全选当前列表及删除前逐项确认。切换动态范围、类型筛选或历史结论显示时清空勾选，未显示的条目不会被一起删除；取消确认不改动记录。
- 删除动态只清除本机通知，保留同步修订记录。同一变化不再出现，新的修订仍会通知；后台同步与删除同时发生时也保留新动态。
- 本地结论新增独立的“删除结论”，与可恢复的“移入历史”区分。批量操作先校验全部条目版本，再统一删除；共享区内容、未选中结论及已加入会话的引用快照保留。删除后历史列表也不显示，重启时不从旧草稿重建。
- 共享区删除统一显示为“共享区已移除”“从共享区移除”，并明确提示本地副本由用户选择保留或删除。
- TypeScript 检查、用户版与管理版构建通过。`tests/conclusions.test.ts`、`tests/collaboration-lifecycle.test.ts` 和 `tests/local-preservation.test.ts` 共 20 项测试全部通过，包含批量版本校验、并发同步及重启持久化。
- `node --import tsx scripts/activity-results-smoke.ts` 通过：复验单条结果和别名，并验证批量选择范围、取消、按别名确认、隐藏历史保留、历史单条删除，以及两次重启后的保留/删除结果。截图位于 `.test-data/activity-results-1789960087242/`。
- 使用隔离的测试共享区与本地数据；已更新 `dist/user`、`dist/admin` 开发构建，本轮未生成新安装包。

# 2026-09-21 单条动态结果、结论别名与本地删除选择

- 动态和提示条按成果 ID 打开单条当前结果，刷新保持范围；原成果不存在时明确提示，不回退到全部公共内容。
- 项目结论支持保存、修改和清除本地别名。别名用于列表、详情、推荐和新增会话引用名称；原名、内容版本和既有会话快照保持不变。
- 共享区删除或合并来源不再自动移除、归档本地结论。删除动态允许保留全部，或按当前版本明确选择移除；其他项目、未选中的多来源结论及既有会话引用不受影响。删除标记防止重启时由旧草稿重新生成条目。
- 类型检查、用户版和管理版构建通过。122 项 Node 用例中，121 项全量通过；另 1 项旧的“自己的删除自动已读”断言按本地副本待处理的新规则更新后，定向复验通过。新增核心回归覆盖别名、独立来源、保留/删除选择及重启。
- `npm run test:activity-ui` 通过：使用独立测试共享区，验证单条查看、过期路径按 ID 定位、刷新与返回、别名编辑/清除/引用、远端删除后的本地选择、重启保留及会话快照保留。截图位于 `.test-data/activity-results-1789957696345/`。

# 2026-09-20 管理端网络出口同步

- 已同步远端 `main` 的 `abf3585`（Add optional admin network egress），保留本地整理、结论、动态、标题编辑及多窗口/重启修复。
- `npm run check` 通过：TypeScript、119 项 Node 测试及两版构建。另补验出口配置与本地会话、未处理动态共同持久化；普通设置保存不会覆盖最新出口配置。
- `test:egress-ui`、`test:connection-ui`、`test:collaboration-ui` 通过，覆盖出口启停/接入码更新、用户端可选入口、原有多窗口及本地数据重启保留。
- 同步前本地修改备份保留在 `codex-before-network-egress-2026-09-20` stash。没有推送本地修改，没有关闭用户现有工作台或改动真实出口配置。
- 真实联网验收待提供管理端接入码，或管理端所在电脑及其直连/上游代理配置；本次没有发送真实模型请求、开放防火墙或重新打包安装程序。

# 2026-09-20 整理与本地状态回归

- `npm run check` 通过：TypeScript、115 项 Node 测试及用户版/管理员版构建。
- `npm run test:session-ui` 通过，增加整理结果名称编辑与保存验证。
- `npm run test:collaboration-ui` 通过：项目文档未操作/已操作分组、切组清空隐藏多选、合并结果自动定位、结论加入两个会话后勾选锁定、关闭并重开用户版后会话/输入/动态/结论及引用状态保持。
- 新增核心回归覆盖精简的三字段生成约定、旧结果验证限制不截断、上传前名称修改与上传后本地名称、并发引用去重、结论快照及同一原生上下文只发送一次。
- 持久化回归覆盖 350 条已读/未读动态、关闭提示但不标已读、已归档结论不在启动时恢复、会话和输入草稿、已上传任务和本地名称。停止按 300 条上限丢弃动态记录。

以上使用隔离测试数据及模型协议替身；没有删除用户现有会话、调用真实业务模型或修改真实共享区。本轮未重新打包安装程序。

# 0.5.0 验证记录

日期：2026-09-16，Windows 11 x64。使用说明和操作矩阵见 [用户与组管理](docs/admin-management.md)。

- TypeScript 检查、37 项 Node 测试、18 项 Python 管理程序测试、两版构建均通过。
- 管理员增加“全部用户”和“按组查看”，显示全部组关系、组内组管理员、未分组用户。支持全局无组/多组创建、组内默认加入、用户加入已有组、组添加已有用户、双向移出、组内授予/撤销组管理员、搜索筛选及重启保存。
- 专项开发界面测试通过 12 组场景，截图已检查，记录位于 `.test-data/admin-management-1789540767768`。最终 EXE 专项测试增加取消操作检查，共 13 组场景通过，记录位于 `.test-data/admin-management-1789541341162/result.json`。首次 EXE 专项运行在隐藏窗口截图环节超时；最终重跑只取消了打包模式的截图采集，所有功能断言、筛选和重启验证均实际完成。
- 新的单组成员变更在后端锁内读取最新状态，分别验证旧管理员快照不会覆盖其他组、无效目标/角色拒绝、移出后在线连接撤权、账号和文件保留、Linux 部分执行恢复后保留其他组新权限。
- 开发及最终 EXE 的原有界面回归通过。最终 SSH 管理测试额外覆盖用户侧加入 NLP 组、组侧移出和 OCR 组管理员保留；用户版登录、独立会话/交接、成果提交、断线及离线重启回归通过。
- 最终 EXE 管理员 + Alice + Bob 的本地共享验证通过，记录位于 `.test-data/local-ui-1789541161887/result.json`。包括文件真实落盘、跨成员预览、私有轨迹、在线启停、文件批准差异展示与双版本接口隔离。
- 两版 EXE 并行启动和独立数据目录检查通过。ASAR 中的 JS/CSS/Python 与最终源构建一致；随包说明一致；ZIP CRC、安装包 SHA256、Cursor 417 个文件及 Codex 6 个文件逐一核对通过。见 [交付哈希](docs/SHA256SUMS-0.5.0.txt)。
- 打包目标改为 `release/0.5.0/admin` 和 `release/0.5.0/user`，避免覆盖正在运行的旧版本。首次旧目录打包遇到已打开的 Alice 用户版文件占用，立即从原 0.4.0 ZIP 仅恢复缺失文件，核对全部旧文件与原分发包一致；运行窗口和个人数据未被关闭或改动。
- 演练的三个启动文件已更新至 0.5.0，保留原有数据目录。新版管理员使用独立客户端数据目录读取真实演练共享区，确认 Alice 组管理员、Bob 普通成员以及两个视图均正常，共享账号登记字节未改变。证据保存在演练目录 `admin-upgrade-0.5.0.json`、`launcher-upgrade-0.5.0.json`。

移出组只解除组关系和对应角色，不永久删除账号、用户组或项目文件。Linux 命令使用测试替身与 SSH 回环验证，未对公司真实服务器执行账号修改；本地权限仍为应用层权限桩。安装包未配置企业签名，另一台电脑迁移尚待实际环境验证。本次没有新发起比赛模型任务。

---

# 0.4.0 验证记录

日期：2026-09-16，Windows 11 x64。逐项场景见 [验收用例](docs/acceptance-0.4.0.md)，使用方法见 [本地文件系统联调](docs/local-filesystem.md)。

- TypeScript 检查、35 项 Node 测试、管理员及用户两版构建通过。Python 管理脚本原有 16 项测试通过。
- 本地共享区使用真实磁盘。用例覆盖管理员初始化、非空目录拒绝、成员与项目组、密码哈希与重置、角色导出、组管理员建项目、跨成员读共享成果、私有轨迹、已连接账号即时撤权、路径越界及 Windows 别名/链接绕过、独立会话和离线恢复。
- 三个 Electron 窗口完成管理员、Alice、Bob 的界面演练：建组、建号、任命组管理员、导出配置、建项目、上传与跨用户预览、登录检测、交接编辑、轨迹归档、停用与恢复。开发构建和最终 EXE 均通过；界面回归使用模拟 CLI。
- 真实 Codex 联调发现文件修改批准消息仅带 itemId，文件 diff 在更早的事件中。已补齐按会话、轮次关联的差异展示；协议测试和最终 EXE 的“展开详情→看到文件/差异→拒绝”用例通过，旧轮次或其他会话的 diff 不会复用。
- 最终 EXE 通过原有 SSH/SFTP 界面回归，包括管理员部分失败恢复、登录恢复、网络异常保留输入、多会话输入/引用/交接隔离、成果仅提交仓库链接与说明、保存失败重试及离线重启。两版可同时运行且接口、数据目录独立。
- 两版 ASAR 与最终构建一致；用户包没有管理员脚本。ZIP 完整性、安装包哈希、随包 Cursor 417 个文件与 Codex 6 个文件逐一核对通过。[交付校验表](docs/SHA256SUMS-0.4.0.txt) 对应本版最终安装包。
- 演练脚本支持中断续跑；要求对应轮次的原生完成事件和报告均存在才跳过调用。输入文本只容许 Git 的 LF/CRLF 转换，其他文本变化及二进制变化均拒绝。相关回归用例通过。

本地权限为应用层模拟，不会创建操作系统账号或修改 ACL；具有共享目录系统权限的人仍可绕过应用直接读文件。Linux ACL、sudo/PAM/sshd 与另一台电脑迁移仍待目标环境验证。安装包未配置代码签名。并发认证问题继续按 [既定方案](docs/auth-concurrency-plan.md) 保留为方案项，没有计入本版已修复项。

真实 Codex 比赛演练已完成：两名模拟团队成员、两个独立原生会话、各两轮实际任务，复用本机一个既有个人 Codex 登录。代码通过本地 Git 接力，共享区保存四份说明和四个真实轨迹 ZIP；原项目及输入资料保持原样，没有向比赛平台提交。最终算法验证通过 25 项单元测试，两方案公开格式检查各 22/22；每方案独立评估两次，逐例 MSE/NMSE 与上轮结果完全一致。指标仅为公开小样本本地对照，不能当作官方 Score 或目标服务器性能。

最后一轮 CLI 曾出现两次流连接中断，内部自动重连后正常完成，没有新增第三轮迭代。归档保留异常及完成事件。协调核对确认四个连续 Git 版本、复核者没有修改算法、双方算法与评测器的 LF 规范化内容与对应 Git blob 一致、原生会话与两轮记录对应、引用哈希一致、轨迹 ZIP 完整。管理员停用已连接 Bob 后下一次共享读取被拒绝，恢复后重新可用；他人轨迹访问被拒绝。

最终管理员、Alice、Bob 三个 EXE 使用真实演练数据同时重开，成员、两轮原生会话、每人两份归档与最终报告预览均通过，无渲染错误。完成后再次运行 `--resume`，两人的用户消息仍各为 2，引用仍为 1/2，轨迹仍各为 2，没有重复模型调用、引用或上传。

演练资料及完整验收结果仅保留在本机 `D:\agent开发\比赛协同联调\run-20260916-02`，未提交到工作台 Git 仓库。`result.json`、`verification.json`、`algorithm-verification.json` 是对应的机器检查结果。

演练脚本额外支持显式 `--resume --recover-reported-round`：仅供操作者已核对产出的连接异常收尾，复用原有引用，记录实际模型尝试次数，增加只读确认而不重新做算法迭代；必须收到正常原生完成事件才登记成功。相关轮次/尝试次数用例通过。本次实际恢复由 CLI 内部完成，未使用该额外模型调用分支。

---

# 0.3.0 验证记录

日期：2026-09-16，Windows 11 x64。逐项场景见 [验收用例](docs/acceptance-0.3.0.md)。

- `npm run check`：TypeScript 检查、26 项 Node 测试和两版构建通过。
- `python -m unittest discover -s tests -p test_admin.py`：16 项 Python 测试通过。新增覆盖初始化恢复、建组/ACL/密码/成员组部分失败、命令已成功但连接中断、UID/GID 变化拒绝接管与恢复记录不保存密码。
- Electron 界面用例覆盖管理员失败步骤展示与恢复、创建时分组/任命、成员开通状态、导出无密码配置；多 session 输入/引用/交接隔离、迟到失败不串会话、草稿跨页面保留、保存失败与重试、退出保存保护、仅链接成果提交、失败重连与离线重启。
- 对真实本地 ZIP 解包检查：成果包只有 `README.md` 与 `manifest.json`，无代码附件、参考文件或本机绝对来源路径；旧草稿附件也不加入上传包。会话归档不含未发送的输入草稿。提交期间锁定草稿，重复提交与迟到编辑被拒绝，提交快照保持一致。
- 成果权限使用 SFTP 回环测试检查：组内可读、不能覆盖别人成果、组外看不到项目、轨迹按账号私有；原作者重新连接修复旧成果权限。该测试不替代 Linux ACL 验证。
- Windows 暂时锁住保存目标时有限重试；永久失败保留原文件、清理临时文件。退出保存失败后仍能继续自动保存，有对应回归用例。
- 问题 4 按要求只给 [并发认证方案](docs/auth-concurrency-plan.md)，含 10 条验收用例；诊断脚本重新复现现有 A 检测被 B 取消的问题，没有计入已修复项目。
- `node scripts/smoke.mjs --packaged` 在最终 EXE 上通过完整管理员/用户界面流程，以及离线重启；保存失败时退出保护、恢复后继续保存均通过。`node scripts/check-packaged.mjs` 验证两版同时启动和接口隔离。
- 两版 ZIP 完整性通过，ASAR 版本均为 0.3.0 且主程序与最终构建一致；管理员脚本与源码一致，用户包不含管理员脚本。随包 Cursor 417 个文件、Codex 6 个文件与本地官方分发源逐一校验一致。
- 安装包及免安装 ZIP 位于 `release/admin` 与 `release/user`。交付文件的大小与哈希以 [SHA256 校验表](docs/SHA256SUMS-0.3.0.txt) 和本地 `release/SHA256SUMS-0.3.0.txt` 为准。

Linux 系统命令通过测试替身执行，没有操作公司的实际账号。尚未执行的目标服务器 ACL、sudo/PAM/sshd 联调、真实个人模型任务、另一台电脑迁移，均列在验收文档中。安装程序未配置代码签名证书。

---

# 0.2.1 验证记录

日期：2026-09-16，Windows 11 x64。

- TypeScript 检查、19 项 Node 测试、7 项 Python 回归测试通过。
- 新增登录检测协议测试：Codex/Cursor 已登录、未登录、凭据配置、网络错误、过期、未知输出与超时；检测不发送模型任务。
- 登录进程去重、取消、成功后复检、旧 CLI 配置结果失效、运行中认证过期和认证数据不进入会话归档。
- Electron 界面覆盖：新建会话切换 CLI、未登录按钮状态、主进程前置校验、点击登录后自动恢复、网络错误与未登录区分、发送失败后保留原任务、会话内重新登录。
- 实际官方 CLI 探测：本机 Codex 返回已登录，Cursor 返回未登录。只输出状态，没有输出账号信息或令牌，没有启动浏览器登录或发送付费任务。
- `node scripts/smoke.mjs --packaged` 在最终 EXE 上完成上述界面流程，两个应用可以同时运行；安装包和 ZIP 已生成，ZIP 完整性及 ASAR 版本/接口隔离检查通过。
- 交付校验值见 `release/SHA256SUMS-0.2.1.txt`；用户版包含完整官方 Cursor/Codex CLI。

模拟登录用于自动化检查，未替用户执行真实浏览器授权。公司 Linux 服务器及另一台电脑的部署验证范围与 0.2.0 相同。

---

# 0.2.0 验证记录

日期：2026-09-16，Windows 11 x64。

- TypeScript 检查通过，15 项 Node 测试通过。
- 7 项 Python 测试通过，覆盖工作目录 ACL 命令、拒绝接管非空目录，以及既有用户管理边界。系统命令使用 mock，未操作真实 Linux 账号。
- SSH/SFTP 回环集成覆盖：两个路径必填、远端拒绝访问时不解锁工作台、组管理员创建中文项目、自动创建轨迹目录、同名拒绝覆盖、失败清理、普通成员发现项目、非管理员/已撤权账号拒绝创建。
- 真实轨迹 ZIP 在回环 SFTP 测试中上传到新项目的 `trajectories/alice/`，个人目录以 0700 创建。
- Electron UI 检查覆盖首次设置界面、必填按钮状态、错误 Linux 路径、验证成功进入工作台、对话框创建项目、创建会话与历史默认手动归档。
- 管理员版和用户版的进程、数据目录、IPC 接口仍独立。
- 两版 0.2.0 最终 EXE 已同时启动验证；首次必填设置、后台未配置时拒绝创建会话、随包 CLI 发现和接口隔离均通过。
- 两版 NSIS 安装包和免安装 ZIP 已生成；ZIP 完整性检查通过，ASAR 版本为 0.2.0，用户包不含管理员脚本，管理员包脚本与源码一致。校验值见 `release/SHA256SUMS-0.2.0.txt`。

回环服务是协议测试夹具，不是 Linux ACL 模拟器。目标 Linux 上的实际 chmod/setfacl、sudo/PAM/sshd 行为，以及另一台电脑的安装迁移仍需接入验证。没有读取或使用生产服务器密码。

以下保留 0.1.0 的历史验证范围；0.2.0 已新增上述功能。

---

# 0.1.0 验证记录

日期：2026-09-15。环境：Windows 11 x64。

已通过：

- `npm run typecheck`：TypeScript 编译检查。
- `npm test`：13 项测试，覆盖 SSH 管理身份验证、组管理员访问边界、密码 stdin 传输、SFTP 中文预览/下载/上传、权限拒绝、路径与会话绑定、文件快照、任务恢复和两种 CLI 的协议处理。
- `python -m unittest discover -s tests -p test_admin.py`：5 项测试，覆盖管理操作白名单、root 权限边界、账号身份变更、命令参数与密码分离、撤销组管理员组时断开旧连接。系统命令使用 mock，未在本机操作 Linux 账号。
- `npm run test:ui`：两版开发构建同时运行、独立接口与数据目录、首次连接界面、创建本地工作会话、编辑交接文件、历史默认手动上传、CLI 设置。
- `scripts/check-providers.ts`：官方 Codex 0.154.0 App Server、Cursor Agent 2026.09.10-fd3934a ACP 真实程序初始化成功；未发送付费模型任务。
- 同一检查分别在开发运行文件和最终 `release/user/win-unpacked/resources/providers` 运行；最终 Cursor 417 个发布文件完整复制，含其自身的原生依赖。
- `node scripts/check-packaged.mjs`：两个最终 Windows EXE 同时启动，首次连接界面正常，运行进程、数据目录和可调用接口相互独立；用户版可发现随包提供的两家 CLI。
- 两版分别生成 NSIS 安装程序和完整免安装 ZIP；交付目录提供 SHA256 校验文件。

未宣称完成：公司 Linux 服务器实际建号、PAM/sudo/SSH 规则与目录权限联调；个人模型账号登录后的真实业务任务；在另一台电脑上的安装迁移。当前没有公司服务器凭据，因此没有进行这些外部验证。

共享空间内容管理按照当前范围暂不实现。总管理员可任命项目组管理员；组管理员可以登录并看到自己被分配的项目，但内容管理页面尚未开放。
