# 用户版连接配置与多工作组验收

日期：2026-09-18。

## 交互约定

- 用户直接填写 SSH 地址、端口、成员账号和密码；不导入管理员配置文件。
- 用户只选择本机工作路径。用户版不显示共享根目录输入、共享目录选择按钮、连接名称编辑或存储类型切换。
- 本机工作路径不会替换共享根目录，二者继续独立存储。
- 登录页只显示当前服务器、端口、本机工作路径、成员账号和密码；不提供“添加服务器”或已保存连接选择器。需要换服务器时直接修改地址或端口。
- 指纹不提供手工输入，正常页面不显示身份状态或技术信息。首次确认后由当前 Windows 账号记住；身份变化时只在错误旁提供“重新确认”。
- 本地联调配置缺失共享根目录时提示重新生成联调配置；不会让用户补填第二处路径。

## 用例

| 用例 | 验证内容 |
| --- | --- |
| 首次打开 | SSH 地址、端口和成员账号默认展开；没有管理员配置导入入口 |
| 旧配置 | `competition.xxx` 单配置不再显示无说明下拉框；共享根目录继续使用原值 |
| 路径选择 | 只选择本机工作路径；不会把工作目录变成共享目录 |
| 旧多连接数据 | 不显示连接选择器，使用最近一次配置；下一次登录后只保留当前连接 |
| 连接更换 | 在同一表单直接修改地址、端口或账号；重启保留成功登录的连接，不保存密码或未提交编辑 |
| 首次手动连接 | 在发送密码前提示确认一次；确认后保存服务器身份，后续连接不再询问 |
| 错误密码 | 服务器地址、端口、成员账号和首次身份确认立即保存；修正密码后重试无需重新填写这些信息 |
| 服务器身份变化 | 停止登录；错误旁显示“重新确认”，正常页面不显示身份说明和技术值 |
| 管理员与两用户 | 管理员创建组和成员；两用户填写各自账号、登录并共享文件、成果、轨迹 |
| 同一账号加入两个组 | 一份配置、一次登录发现 OCR 与 NLP 两个组；同名项目独立显示；不显示未授权组 |
| 各组不同身份 | OCR 组管理员与 NLP 普通成员同时生效；NLP 越权创建在后端被拒绝 |
| 切换工作组 | 原会话保留原项目绑定，不随导航切换改变上传目标 |
| 撤销组权限 | 刷新后移除对应组入口；原会话不能继续向该组上传 |
| 空组与未分组 | 未分组用户无工作台；首次组管理员项目引导、草稿恢复及成员身份隔离 |
| SSH 回归 | 回环 SSH/SFTP 测试服务验证错误密码、正常登录、会话建立和重启恢复 |

执行命令：`npm run typecheck`、`npm run build`、`npm run test:connection-ui`、`npm run test:local-ui`、`npm run test:workgroups-ui`、`npm run test:onboarding-ui`、`npm run test:ui`。

本次结果：上述检查全部通过。SSH 流程同时完成保存失败重试、会话输入隔离及离线授权内重启恢复。更新了旧附件用例，使其区分固定引用的项目资料与用户手动选择的附件。

本地用例在独立 `.test-data` 子目录创建真实文件和权限桩；CLI 使用测试夹具。这些用例验证应用行为，不代表真实 Linux 系统权限验收。结果与截图保存在对应测试目录；多工作组额外保存 `two-groups.png` 和 `independent-roles.png`。

## 界面文案清理

删除用户版与管理员版中重复解释界面流程的提示，覆盖登录、本机路径、会话、项目初始化、公共成果、成果整理、账号和组管理。保留字段标签、实际状态、错误信息、上传范围及权限变更的必要后果。登录页不再出现“共享空间由账号权限决定”和本机路径的额外说明；界面中不展示已登录状态和成功读取额度的重复解释。

类型检查、构建及以下界面用例通过：`test:connection-ui`、`test:admin-ui`（15 个操作用例）、`test:session-ui`、`test:onboarding-ui`、`test:permissions-ui`、`test:workgroups-ui`、`test:collaboration-ui`、`test:ui`。已检查登录页、管理员页、成果页截图。
