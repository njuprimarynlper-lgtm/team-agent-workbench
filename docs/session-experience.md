# 会话、成果整理与轨迹上传

本页补充说明会话、整理结果和轨迹上传的交互细节。完整操作顺序见 [使用指导](user-guide.md)，构建与启动见 [部署指导](deployment-guide.md)。默认使用 `%APPDATA%\TeamAgentUser`；若设置 `WORKBENCH_DATA_DIR`，沿用指定数据目录。不要同时启动两个使用同一数据目录的用户进程。

## 用户操作

1. 新建会话先选择 Codex / Cursor。登录状态展示 CLI 返回的邮箱；Codex 还展示返回的套餐。已经登录时，“登录个人账号”禁用；“重新检测”可刷新外部 CLI 的登录变更。没有账号字段时说明“CLI 未提供账号名称”，不会把 Windows 或共享空间账号当作模型账号。
2. 在同一对话框选择模型，查看账号额度；确认工作目录和远端项目后创建。模型固定在会话上，重启、恢复和后台成果整理都沿用该模型。选择“沿用 CLI 默认模型”则保留 CLI 自己的默认行为。列表读取失败不阻止沿用默认模型。
3. 会话工具栏提供“关闭会话”。运行中先提示停止；关闭后保留历史、未发送输入、原生 session ID 和阶段摘要。左侧“已关闭会话”可以查看并重新打开。重新打开不自动重放任务，需主动发送。进程停止不能撤销已经完成的文件修改。
4. 输入框底部的“Skill / 插件”按当前 CLI 和工作目录读取可用能力。选择项只附到下一条消息，发送成功后清空；未发送选择与文字、参考文件一起按会话保存。Codex 使用原生结构化 Skill/App 输入，Cursor 使用 ACP 暴露的 `/` 命令和 CLI 已加载的 MCP 工具。
5. 从会话点击“整理成果”会直接打开对应任务详情；从左侧点击“整理任务”则先显示任务列表，包含来源、项目、类型、状态、时间和结果数量，点击任务后才进入详情。详情显示后台整理状态、已用时间和来源会话，不新增可见会话，不阻塞原会话。直接从会话打开时，“返回‘会话名’”回到原会话；从列表打开时返回任务列表。整理继续在后台运行，只有“停止整理”会终止本次整理。同一会话存在未提交任务时，重复点击会打开最新任务。
6. AI 自动生成标题、Markdown 成果说明和可选仓库链接。页面只保留状态、结果、可选补充、上传位置和操作。上传位置自动识别并显示友好名称；整理失败且尚未确定时明确显示“尚未确定”。“仓库与代码版本（可选）”默认收起；“给团队的补充（可选）”独立自动保存。保存成功不常驻提示，失败时显示错误与重试。仓库链接可留空；Git 信息只记录状态，不上传代码。失败重试使用原冻结材料；已完成任务的“再次整理”会先让用户选择范围：增量只处理上次快照后的新增消息，全量重新读取当前完整会话。两种范围均新建任务并保留原任务；没有新增消息时增量选项不可用。
7. “轨迹上传”预览采集的会话内容，默认手动。绑定项目的工作会话可开启每轮结束自动上传；上传到项目的 `trajectories/账号/`。移除“会话归档”和本地导出入口。仍保留正常的本地会话记录，以及失败上传重试所需的队列缓存，这不是额外的用户归档操作。

成果只上传成果说明、可选 GitHub 仓库链接和提交元数据，不上传代码或材料快照。无链接时不会生成空的仓库占位行。新包采用 `schemaVersion: 3`、`kind: project-contribution`，`repoUrl` 可缺省；旧包保留原样。

自动目录选择：程序在当前绑定项目中最多扫描 40 个目录、深度 3 层，读取小型 README.md / README.txt 的前 2000 字作为分类说明。排除隐藏目录、轨迹目录、链接和他人的成果目录。AI 只返回已枚举的目录 ID，不能自行构造路径；无法匹配或目录读取不完整时保留默认成果目录兜底。上传使用原项目和账号绑定，仍由本地权限桩或 Linux/SFTP 权限决定是否允许写入。目录可读不代表可写，权限拒绝会显示在传输记录中，不能绕过。

旧草稿保留原来已编辑的正文；未提交草稿迁移到默认成果目录，已提交成果保持冻结。新成果包合并 AI 正文与非空的人类补充，只包含 README.md 和 manifest.json。


## 阶段摘要与整理结果

- **阶段摘要**：会话内的本地过程资料，记录目标、发现、依据和待办。入口位于默认收起的“AI 参考内容”，打开后先只读预览；有遗漏时通过“更正摘要”编辑。摘要本身不随成果上传。
- **整理结果**：点击“整理成果”后，AI 根据阶段摘要、冻结对话和参考资料另行提炼、供团队阅读的分享内容。用户审阅、可选补充并确认上传。已有结果不会被覆盖；需要再次整理时选择增量或全量并生成新任务。
- **本地项目成果库**：本地整理完成后的勾选项进入当前账号、当前项目的成果列表，保留“本地整理”来源；与共享区导入、手工记录处于同一列表，是否上传不影响成果是否存在。内容类别仍为项目结论、项目标准、方法探索、问题与风险、改进建议。成果随账号同步，Session 仍只保存在本机。左侧“成果整理”查看整理任务及其状态，“团队项目成果库”供所有成员阅读和引用共享成果，组管理员有额外维护操作。
- 底层继续使用已有 `handoff.md` 文件、会话绑定及读写接口。旧文件首行“Agent 工作记录”在展示时温和转换为“阶段摘要”，无需用户手工迁移。

## 接入来源与边界

| 能力 | Codex | Cursor |
| --- | --- | --- |
| 身份 | App Server `account/read`，白名单提取 `email` / `planType` | `status --format json`，白名单提取 `userInfo.email` |
| 模型列表 | `model/list` 分页，使用 `model` 字段，不使用展示 ID | `agent models`，解析当前 CLI 的模型行；无法识别时显式提示 |
| 模型传递 | `thread/start` / `thread/resume` 与 `turn/start` 的 `model` | ACP 新建/恢复后调用 `session/set_model`，传递 `modelId` |
| Skill | `skills/list`；`turn/start` 同时发送 `$name` 与结构化 `skill` 项 | ACP `available_commands_update`；消息以 `/name` 调用 |
| 插件工具 | `plugin/installed`；发送 `@plugin` 与 `mention(plugin://name@marketplace)` | CLI 自动加载个人/项目 MCP；工作台从 `.cursor/mcp.json` 显示可选工具 |
| 额度 | `account/rateLimits/read`，优先显示所有 `rateLimitsByLimitId`，回退旧字段 | 当前 CLI 没有公开个人套餐额度读取接口；按钮打开官方 Spending 页 |

Codex 百分比是“已用”，界面显示 `100 - usedPercent` 的剩余额度和 Unix 秒重置时间。它是账号/用量池信息，不推断模型独享配额。不支持的认证方式或网络失败显示不可用，绝不显示虚构的 0% 或无限额度。额度查询失败不覆盖可用模型列表，也不改变认证状态。身份和额度只留在内存，不写入成果或轨迹。

参考：[Codex App Server](https://learn.chatgpt.com/docs/app-server)、[Cursor CLI 参数](https://cursor.com/docs/cli/reference/parameters)、[Cursor ACP](https://cursor.com/docs/cli/acp)、[Cursor 官方用量说明](https://cursor.com/help/models-and-usage/usage-limits)。Cursor `session/set_model` 同时核对本仓库随带官方 CLI 的 ACP 实现；升级 CLI 后应执行协议回归。

## 验证入口

- `npm run check`：类型检查、全部 Node 测试、两个开发版构建。覆盖身份、模型与额度、会话生命周期，以及自动目录识别、默认目录回退、权限变化、超时、无效输出、补充保留、旧草稿迁移与成果包内容。
- `npm run test:session-ui`：两种提供方的窗口操作回归，登录按钮禁用、模型与额度、整理等待/离开/返回/完成提示、重复点击、停止/重试、可选补充、缺失链接更正、展开说明、操作确认、关闭/恢复和移除本地轨迹导出。
- `npm run test:ui`：原有用户/管理员隔离、SFTP 共享、成果提交和编辑保存恢复回归。
- `npm run test:local-ui`：管理员与两个成员、本地共享文件系统、项目创建及轨迹实际上传。

真实账号只读联调已验证本机 Codex 的身份、模型列表与额度，不发送付费模型任务。本机 Cursor 尚未登录，Cursor 模型选择与会话交互使用协议模拟 CLI 验证，不能当作真实账号端到端验证。

0.6.3 用例：`tests/usability.test.ts` 覆盖无链接、有链接、非法链接和空说明；`tests/preparation.test.ts` 验证无链接结论的实际上传及同组可见；`scripts/local-smoke.mjs --accounts --packaged` 在两个用户和管理员窗口中验证无链接上传按钮、补充说明、实际 ZIP 内容和同事访问。

本次用例：`tests/preparation.test.ts` 验证停止整理不关闭原工作会话、补充与摘要保留、重试与重开；`scripts/session-experience-smoke.mjs` 验证阶段摘要默认收起且只读，以及运行中返回继续、显式停止、完成、失败和等待 CLI 确认。`scripts/smoke.mjs` 保留阶段摘要更正的保存失败恢复回归。

2026-09-17 开发构建验证：90 项 Node 测试通过；`scripts/session-experience-smoke.mjs`、`scripts/preparation-permissions-smoke.mjs`、`scripts/local-smoke.mjs`、`scripts/smoke.mjs` 全部通过，包括用户/管理员窗口、两个成员共享、阶段摘要更正、后台整理、显式停止、中文失败提示、保存失败恢复和重启恢复。截图位于 `artifacts/agent-work-notes.png`、`artifacts/preparation-ready.png`、`artifacts/preparation-compact.png`。本次未重打安装包。
