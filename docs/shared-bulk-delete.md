# 团队成果批量删除

- “团队项目成果库”提供“批量删除团队成果”。组管理员可选择当前项目内的成果、文件和轨迹；普通成员只能选择自己尚未被组管理员整理的提交。无权删除的条目不可勾选，服务端仍逐项校验实际权限。
- 选择只作用于当前分组和筛选结果。全选最多 100 项；切换分组、搜索、类型、项目或刷新会清空选择，与语义合并互斥。
- 确认框列出实际选中的标题、提交人和版本，并说明删除不可从共享库恢复。删除共享条目不会删除已保存的本地成果、已有 Session 引用或整理记录；合并来源也不会自动恢复。附件存储沿用已有保留策略，不顺带回收其他记录可能引用的附件。
- 提交前校验整个选择的项目、版本和权限；复用现有单条删除接口逐项执行，每项由服务端再次检查。中途出现冲突、权限变化、断线或保存异常时停止后续操作，显示已完成与未完成的数量。已经完成的删除不回滚。
- 丢失应答时只读核对一次：确认远端已移除的计入完成，无法核实的明确提示结果不确定。不会自动重试删除，用户刷新核对后可重新选择并确认。
- 本次不增加服务端接口，不需要为批量删除单独升级服务端。分类显示按用户确认保持原样：缺少分类字段时仍使用“项目成果”作为缺省标签。

验证使用临时模拟团队、权限规则、持久化 Session 和静态页面渲染，不连接真实共享空间执行删除，也不在用户桌面运行界面测试。

2026-09-22：178 项后台回归通过；新增用例覆盖批量权限/版本校验、部分失败与未知结果、重复请求、临时撤权、文件/轨迹删除、本地记录保存失败，以及引导确认/取消/过期轮次。类型检查与隔离构建通过。
